package config

const (
	//DBHost is the hostname of postgres DB
	DBHost = "postgresql.cqjyvunak5ku.us-east-2.rds.amazonaws.com"

	//DBUserName is the user name of postgres DB
	DBUserName = "postgres"

	//DBPassword is the password of postgres DB
	DBPassword = "??v??6?6???"

	//DBName is the name of postgres DB
	DBName = "postgres"

	//DBPort is the port for postgresql
	DBPort = 5432

	//Port is where our service is running
	Port = "8003"
)
